var numeros = [1, 2, 3, 4, 5, 7, 8, 9,"aluno", 34,"none",[2,3,4,5,6,7,7,1,2,3,4,4,]];

console.log(numeros[2]);

for (var pos = 0; pos < numeros.length; pos++) {
    console.log("Posicao: " + numeros[pos]);
}